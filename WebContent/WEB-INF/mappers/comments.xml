<?xml version="1.0" encoding="UTF-8" ?>
 <!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="opinion">
<!-- 댓글 저장하는거 -->
	<insert id="addByComments" parameterType="java.util.HashMap">
		insert into opinion values(opinion_seq.nextval, #{choice}, #{ment}, #{talker}, #{ino}, #{code})
	</insert>

<!-- 특정 게시글 댓글 전부 뽑는거(오름차순) -->
	<select id="getAllByComments" resultType="java.util.HashMap" parameterType="java.lang.Integer">
		select * from opinion where ino=#{ino} order by no asc
	</select>
	
<!-- 
	특정 게시글 댓글의 댓글 뽑는거 
	<select id="getAllByComments" resultType="java.util.HashMap" parameterType="java.lang.String">
		
	</select>
-->
</mapper>

